# app.py
from data_preprocessor import DataPreprocessor
from model_trainer import ModelTrainer
from inference_agent import InferenceAgent
from user_interaction import UserInteraction

# Initialize agents
data_preprocessor = DataPreprocessor()
model_trainer = ModelTrainer()
inference_agent = InferenceAgent(model)
user_interaction = UserInteraction(inference_agent)

# Example usage
raw_data = pd.read_csv('data.csv')
cleaned_data = data_preprocessor.preprocess(raw_data)
trained_model = model_trainer.train(cleaned_data, model, training_args)
user_input = "Your query here"
response = user_interaction.handle_request(user_input)
print(response)
